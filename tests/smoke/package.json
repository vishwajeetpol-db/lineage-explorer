{
  "name": "lineage-explorer-smoke-tests",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright smoke tests run against a deployed app URL before customer-facing releases",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "install:browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.3.3"
  }
}
