frontend/node_modules/
.venv/
__pycache__/
*.pyc
*.png
ss_*
screenshot_*

# Sensitive / local files
worspace_details_access_tokens
table_dependancy_graph
.env
.env.*
*.pem
*.key

# Local-only test scaffolding (kept on disk for local runs, never shipped or committed).
# Reasons: not wired into any CI; contains workspace-specific defaults; would otherwise
# bake customer values into versioned code and violate the "zero-code, any-workspace" deploy promise.
tests/
backend/tests/
monitoring/
pytest.ini
requirements-dev.txt
refresh_job/

# Local-only demo/setup scripts (run by author against own workspace; not for customer envs).
setup_demo_lineage.py
create_stress_test.py
generate_stress_test.py

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Databricks local state
.databricks/
.claude/
